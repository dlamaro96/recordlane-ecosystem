# Contributing

Copy `template`, implement declared capabilities, keep credentials out of fixtures, and run `pytest`. A recipe PR must state exact API surface, direction, checkpoints, deletions, consistency, limitations, and whether live vendor validation occurred.
